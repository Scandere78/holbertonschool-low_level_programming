#include "main.h"

/**
 * _abs - Écrire une fonction qui calcule la valeur absolue d’un entier.
 *@n: function parameter to be checked
 * Return: -n or n;
 */
int _abs(int n)
{
	if (n < 0)
		return (-n);
	else if (n >= 0)
	{
		return (n);
	}
}
